// import mqttClient from "../config/mqtt";

// const publish = (topic, message) => {
//     mqttClient.publish(topic, message, { qos: 0 }, (err) => {
//         if (err) {
//             console.log(`[publisher.js] publish() error: ${err.message}`)
//         }
//     })

// }